# game "A = B" Interpreter
## Usage
write "A=B" code at "./code/{filename}.ab"

### run
at src directory, run
`python3 ./interpreter.py <filename> <input> [-v] [--maxloop <maxloop>]`

### option

 -v: verbose mode  
 --maxloop: maxloop